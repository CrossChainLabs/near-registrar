{
  "name": "near-registrar",
  "private": true,
  "description": "Implementation of TLAs using Rust",
  "author": "Near, Inc. <hello@near.org>",
  "license": "(MIT AND Apache-2.0)",
  "scripts": {
    "build": "yarn build:rs",
    "build:rs" : "./build.js",
    "clean": "rm -rf ./out && cargo clean)",
    "test": "cargo test -- --nocapture --color always"
  },
  "devDependencies": {
    "jest": "^26.3.0",
    "near-sdk-as": "^0.4.2",
    "near-cli": "^1.0.2",
    "shelljs": "^0.8.4"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
